# Hosted build (Netlify free tier). Processing is fully in-browser, so these
# limits protect the visitor's machine, and the hint text points people with
# bigger videos to the desktop app instead.
VITE_BUILD_TARGET=web
VITE_MAX_VIDEO_MB=1000
VITE_MAX_VIDEO_MIN=30
